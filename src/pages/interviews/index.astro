---
import { getCollection } from 'astro:content';
const posts = await getCollection('interviews');
---
<h1>起業家インタビュー</h1>
<ul>
  {posts.map((p) => (
    <li><a href={`/interviews/${p.slug}/`}>{p.data.title}</a></li>
  ))}
</ul>
