---
import { getCollection } from 'astro:content';
const posts = await getCollection('vc-interviews');
---
<h1>VC インタビュー</h1>
<ul>
  {posts.map((p) => (
    <li><a href="/vc-interviews/{p.slug}/">{p.data.title}</a></li>
  ))}
</ul>
