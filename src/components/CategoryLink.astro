---
export interface Props { href:string; label:string; icon:string }
const { href, label, icon } = Astro.props;
---
<a href={href} class="flex items-center gap-3 p-4 bg-white rounded shadow hover:bg-indigo-50">
  <span class="text-2xl">{icon}</span>
  <span>{label}</span>
</a>
